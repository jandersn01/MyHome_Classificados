package padrao_Builder;

public interface AnuncioBuilder {
	/* +reset():AnuncioBuilder
	+setTitulo(String): AnuncioBuilder
	+setDescricao(String): AnuncioBuilder
	+setPreco(double): AnuncioBuilder
	+setTipoAnuncio(TipoAnuncio): AnuncioBuilder
	+setImovel(Imovel): AnuncioBuilder
	+setEndereco(Endereco): AnuncioBuilder
	+setAnunciante(Anunciante): AnuncioBuilder
	+adicionarFoto(String): AnuncioBuilder
	+build(): Anuncio */
}
